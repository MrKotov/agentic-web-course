# Secrets — never commit. Each lecture folder ships .env.example instead.
.env
.env.local
.env.*.local

# Node
node_modules/
dist/
*.tsbuildinfo

# Test / grading artefacts
result.json
coverage/

# OS / editor
.DS_Store
Thumbs.db
*.swp
