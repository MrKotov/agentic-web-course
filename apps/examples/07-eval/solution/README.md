# solution/ — 07-eval

Тук няма решение в `main`.

Коментирана версия на намирките е на клон `solutions/07` и се слива в `main`
**след крайния срок**. Докато срокът тече, тази папка съдържа само тази бележка.

За `07-eval` „решението" е списъчък на намирки + доказване че всяко е истински или лъжа,
всичко това в `exercise/`.
