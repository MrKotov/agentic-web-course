# demo/ — какво пуска преподавателят

```bash
npm run demo
```

Чете `recordings/hello-world.json`. Нула мрежови заявки, нула ключове. Работи и при
изключен интернет в залата — това е изискване, не удобство.

За истинска заявка (извън лекция, с попълнен `.env`):

```bash
npm run demo -- --live
```
