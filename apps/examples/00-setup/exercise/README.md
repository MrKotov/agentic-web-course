# exercise/ — 00-setup

Няма код за писане. Задачата е средата да работи.

```bash
cp ../.env.example ../.env   # после попълнете ключа
npm run check
```

Готови сте, когато `npm run check` излиза с код 0 и нито един ред `ГРЕШ`.

Това не се оценява като упражнение — но всяко следващо упражнение го приема за даденост.
