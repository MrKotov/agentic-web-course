# solution/ — 06-ship

Тук няма решение в `main`.

Решенията на упражненията се публикуват на клон `solutions/06` и се сливат в `main`
**след крайния срок** на упражнението. Докато срокът тече, тази папка съдържа само тази
бележка.

За `06-ship` „решението" е Dockerfile + GitHub Actions workflow + живия URL (връщащ 200) +
производствен доклад, всичко това в `exercise/`, които минават тестовите си套措 изцяло.
