/**
 * Not built yet. Spec: handover/01-course-site.spec.md, "Interactive components", item 3
 * ("Tokenizer view. Text in, token boundaries out.").
 */
export default function TokenizerViewPlaceholder(): React.ReactElement {
  return (
    <div className="course-todo" role="note">
      <p className="course-todo__label">TODO: изглед на токенизация</p>
      <p className="course-note">
        Текст на вход, границите на токените на изход. Виж handover/01-course-site.spec.md,
        раздел „Interactive components“, точка 3. Не е построен в този проход.
      </p>
    </div>
  );
}
